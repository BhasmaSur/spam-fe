{
  "name": "@spammetwice/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start --scope=@spammetwice/web",
    "bootstrap": "lerna bootstrap --hoist",
    "build": "lerna run build --scope=@spammetwice/web"
  }
}
