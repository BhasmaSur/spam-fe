//import httpService from '../httpService';

const LoginService = {
  // getEmailData: (data) => httpService('users/emailInfo', 'post', data),
  // getMfaRemember: (username) => httpService(`mfaRemember/${username}`, 'get'),
  // setMfaRemember: (data) => httpService('mfaRemember', 'post', data),
};

export default LoginService;
