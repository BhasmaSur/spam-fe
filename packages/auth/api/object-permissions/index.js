//import httpService from '../httpService';

const ObjectPermissionService = {
  // getObjectPermissions: (roles) =>
  //   httpService('roles/objectsandpermissions', 'post', roles, 'rbac'),
};

export default ObjectPermissionService;
